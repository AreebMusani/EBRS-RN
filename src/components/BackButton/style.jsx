import {StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    arrowBack: {
        position: "absolute",
        left: 20,
        top: 20,
        zIndex: 20
    }
})

export default styles;
